# Plan 01-02: Clean BackgroundRenderer + SdfParamRegistry

## Objective

清理 BackgroundRenderer.ts 中的死代码和平台相关方法，清理 SdfParamRegistry 中的平台 uniform 条目。

## Tasks

### 1. 删除 BackgroundRenderer.ts 中的 testFragmentShader 块

**File:** `.worktrees/phase-01-sdf-background/frontend/src/components/tree/scene/BackgroundRenderer.ts`

删除 lines 80-156（`const testFragmentShader = /* glsl */ \`...`）。这是一个从未被使用的 debug shader（material 用的是 `backgroundFragmentShader`）。

### 2. 移除 BackgroundRenderer.ts 中的平台相关 import

移除：
- `import { SDF_PLATFORMS } from '../shaders/sdfPlatforms';`
- `import { TRIPLANAR_MAPPING } from '../shaders/triplanarMapping';`

保留：
- `import { SDF_PRIMITIVES } from '../shaders/sdfPrimitives';` — 仍被 vista 使用
- `import { SDF_ARCHITECTURE } from '../shaders/sdfArchitecture';` — 被 mapSakura 使用

### 3. 删除 setPlatformType() 和 setPlatformZ() 方法

**File:** `.worktrees/phase-01-sdf-background/frontend/src/components/tree/scene/BackgroundRenderer.ts`

删除：
```typescript
/** Debug: directly set platform type uniform (0-4) without going through params. */
setPlatformType(type: number): void {
  const t = Math.round(Math.max(0, Math.min(4, type)));
  this.material.uniforms.uPlatformType!.value = t;
}

/** Debug: directly set platform Z distance uniform (2-5). */
setPlatformZ(z: number): void {
  this.material.uniforms.uPlatformZ!.value = Math.max(2, Math.min(5, z));
}
```

### 4. 清理 SdfParamRegistry 中的平台条目

**File:** `.worktrees/phase-01-sdf-background/frontend/src/components/tree/scene/SdfParamRegistry.ts`

移除 `uPlatformType` 和 `uPlatformZ` 的注册条目。

### 5. 清理 DebugGUI 中的平台控制

**File:** `.worktrees/phase-01-sdf-background/frontend/src/components/tree/scene/DebugGUI.ts`

搜索并移除对 `setPlatformType` / `setPlatformZ` 的调用（如果存在）。

### 6. 更新 backgroundRenderer.spec.ts

**File:** `.worktrees/phase-01-sdf-background/frontend/src/components/tree/__tests__/backgroundRenderer.spec.ts`

- 移除对 `uPlatformType` / `uPlatformZ` uniform 的断言（如果存在）
- 移除对 `setPlatformType` / `setPlatformZ` 方法的测试（如果存在）

### 7. 检查其他引用

```bash
grep -rn "uPlatformType\|uPlatformZ\|setPlatformType\|setPlatformZ\|SDF_PLATFORMS\|TRIPLANAR_MAPPING\|sdfPlatforms\|triplanarMapping" \
  .worktrees/phase-01-sdf-background/frontend/src/ \
  --include="*.ts" --include="*.vue" \
  | grep -v "node_modules" | grep -v ".spec.ts"
```

如果有遗漏引用，一并清理。

## Verification

```bash
cd .worktrees/phase-01-sdf-background/frontend
npx vitest run --reporter=verbose 2>&1 | tail -20
npx tsc --noEmit 2>&1 | tail -10
grep -rn "uPlatformType\|uPlatformZ\|setPlatformType\|setPlatformZ\|SDF_PLATFORMS\|TRIPLANAR_MAPPING\|sdfPlatforms\|triplanarMapping" src/ --include="*.ts" --include="*.vue" | grep -v node_modules
```

- TypeScript 编译无错误
- 所有测试通过
- grep 无结果（所有引用已清理）

## Requirements Covered

CLN-04, CLN-05, CLN-07
