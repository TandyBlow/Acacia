---
phase: 01-camera-system-shader-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/tree/scene/BackgroundRenderer.ts
  - frontend/src/components/tree/scene/SceneManager.ts
  - frontend/src/components/tree/__tests__/backgroundRenderer.spec.ts
autonomous: true
requirements: [CAM-03, CAM-04, CAM-05]

must_haves:
  truths:
    - "BackgroundRenderer 构造函数使用 SdfParamRegistry.createUniforms() 创建所有注册管理的 uniforms"
    - "BackgroundRenderer.updateParams() 调用 applyParamsToUniforms() 将 TreeStyleParams 中的 bg* 值写入 shader uniforms"
    - "风格切换时，THEME_PRESETS 中的 bgCamY/bgCamPitch/bgCamZ/bgFovZoom 通过 applyParamsToUniforms 流到 shader——CAM-03/CAM-04 数据链路贯通"
    - "SceneManager 监听容器 mousemove 事件，计算归一化 mouseUV 值（x = clientX/width, y = 1 - clientY/height）"
    - "BackgroundRenderer.updateMouseUV() 将 mouseUV 写入 shader uniform uMouseUV——CAM-05 数据链路贯通"
    - "updateMouseUV() 使用 Math.max(0, Math.min(1, val)) 将 x,y clamp 到 [0,1]，并使用 Number.isFinite() 守卫防止 NaN/Infinity"
    - "SceneManager.disposeScene() 中正确移除 mousemove 事件监听器"
  artifacts:
    - path: "frontend/src/components/tree/scene/BackgroundRenderer.ts"
      provides: "重构后的 SDF 背景渲染器——使用 SdfParamRegistry 统一管理 uniforms，新增 updateParams() 和 updateMouseUV()"
      exports: ["BackgroundRenderer"]
      min_lines: 110
    - path: "frontend/src/components/tree/scene/SceneManager.ts"
      provides: "新增鼠标事件监听——onMouseMove 计算 mouseUV 并传递给 BackgroundRenderer"
      contains: "this.backgroundRenderer.updateMouseUV"
    - path: "frontend/src/components/tree/__tests__/backgroundRenderer.spec.ts"
      provides: "CAM-03/CAM-05 自动化测试——验证 Registry 集成和 mouseUV 写入"
  key_links:
    - from: "BackgroundRenderer constructor"
      to: "SdfParamRegistry.createUniforms()"
      via: "const registryUniforms = createUniforms(); ...uniforms: { ...registryUniforms, ... }"
      pattern: "createUniforms\\(\\)"
    - from: "BackgroundRenderer.updateParams"
      to: "SdfParamRegistry.applyParamsToUniforms"
      via: "applyParamsToUniforms(this.material.uniforms, params)"
      pattern: "applyParamsToUniforms\\(this\\.material\\.uniforms"
    - from: "TreeStyleParams.bgCamY/bgCamPitch/bgCamZ/bgFovZoom"
      to: "shader uniforms uCamY/uCamPitch/uCamZ/uFovZoom"
      via: "SdfParamRegistry tsKey 映射 → applyParamsToUniforms → material.uniforms"
    - from: "SceneManager.onMouseMove"
      to: "BackgroundRenderer.updateMouseUV"
      via: "this.backgroundRenderer.updateMouseUV(this.mouseUV)"
      pattern: "updateMouseUV\\(.*mouseUV"
    - from: "backgroundRenderer.updateMouseUV"
      to: "material.uniforms.uMouseUV.value"
      via: "this.material.uniforms.uMouseUV!.value.set(x, y)"
      pattern: "uMouseUV.*\\.value\\.set"
    - from: "SceneManager.disposeScene"
      to: "renderer.domElement"
      via: "removeEventListener('mousemove', ...)"
      pattern: "removeEventListener\\('mousemove'"
</must_haves>

<objective>
**任务:** 重构 BackgroundRenderer 集成 SdfParamRegistry（CAM-03），打通 TreeStyleParams 中 bgCam* 参数到 shader 的完整数据链路（CAM-04），添加鼠标视差深度效果（CAM-05）。

**目的:** 消除当前 BackgroundRenderer 手动维护 uniform 列表的反模式，使相机参数从主题预设自动流到 shader。鼠标移动驱动远景层视差偏移，增加场景纵深感。

**产出:** 修改后的 BackgroundRenderer.ts、SceneManager.ts、新建的 backgroundRenderer.spec.ts。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/components/tree/scene/BackgroundRenderer.ts
@frontend/src/components/tree/scene/SceneManager.ts
@frontend/src/components/tree/scene/SdfParamRegistry.ts
@frontend/src/constants/theme.ts
@.planning/phases/01-camera-system-shader-foundation/01-RESEARCH.md

<interfaces>
<!-- 关键接口——执行器直接使用，无需代码库探索 -->

来自 SdfParamRegistry.ts（关键导出）:
```typescript
export interface SdfParamEntry {
  name: string;          // 如 'uCamY', 'uSkyTopColor'
  glslType: 'vec3' | 'float' | 'int';
  tsKey: keyof TreeStyleParams;  // 如 'bgCamY', 'skyTopColor'
  defaultValue: number | [number, number, number];
  category: 'color' | 'geometry' | 'camera' | 'fog';
  min?: number;
  max?: number;
  step?: number;
  uiLabel?: string;
}

export const SDF_PARAM_REGISTRY: SdfParamEntry[];
// 包含 15 个 entry：color(3) + camera(4) + geometry(7) + fog(1)
// camera 4 个: {name:'uCamY', tsKey:'bgCamY', defaultValue:2.8},
//              {name:'uCamPitch', tsKey:'bgCamPitch', defaultValue:-0.15},
//              {name:'uCamZ', tsKey:'bgCamZ', defaultValue:-6.0},
//              {name:'uFovZoom', tsKey:'bgFovZoom', defaultValue:1.8}

export function createUniforms(): Record<string, THREE.IUniform>;
// 返回: { uSkyTopColor: {value: THREE.Color}, uCamY: {value: 2.8}, ... }
// 注意 vec3 类型使用 THREE.Color 包装，float/int 类型直接赋值 .value

export function applyParamsToUniforms(
  uniforms: Record<string, THREE.IUniform>,
  params: TreeStyleParams,
): void;
// 遍历 SDF_PARAM_REGISTRY，对于每个 entry:
//   vec3: uniforms[entry.name].value.set(params[entry.tsKey][0], [1], [2])
//   float/int: uniforms[entry.name].value = params[entry.tsKey]
```

来自 BackgroundRenderer.ts 当前结构（需重构）:
```typescript
// 当前存在的问题:
// 1. 构造函数硬编码 11 个 uniforms（第 31-42 行）——未使用 createUniforms()
// 2. update() 手动逐个设置 9 个 uniforms（第 65-76 行）——未使用 applyParamsToUniforms()
// 3. paramsFromTheme() 只提取 7 个 bg* 参数（第 95-113 行）——遗漏 bgCamY/Pitch/Z/Zoom
// 4. 无 updateMouseUV() 方法 —— CAM-05 需要新增

export interface BackgroundUniformParams { ... }  // 将被弃用
export class BackgroundRenderer {
  constructor(styleType: number, seed: number);
  getMesh(): THREE.Mesh;
  getMaterial(): THREE.ShaderMaterial;
  update(params: BackgroundUniformParams): void;
  updateTime(time: number): void;
  updateSize(w: number, h: number): void;
  dispose(): void;
  static paramsFromTheme(theme: TreeStyleParams, styleType: number, seed: number): BackgroundUniformParams;
  static styleToType(style: string): number;
}
```

来自 SceneManager.ts 关键区域（需修改）:
```typescript
export class SceneManager {
  private backgroundRenderer: BackgroundRenderer | null = null;
  private container: HTMLElement;

  // buildScene() — 第 114 行（需在 setupCameraAndRenderer 后添加 mousemove 监听）
  // createBackground() — 第 523-534 行（调用 paramsFromTheme + update，需改为 updateParams）
  // applyStyleParams() — 第 737-806 行（第 773-778 行调用 paramsFromTheme + update，需改为 updateParams）
  // disposeScene() — 第 935-1006 行（需添加 removeEventListener('mousemove', ...)）

  // 需要新增:
  // private mouseUV = { x: 0.5, y: 0.5 };
  // private onMouseMove = (event: MouseEvent) => { ... };
}
```

来自 TreeStyleParams (theme.ts):
```typescript
export interface TreeStyleParams {
  // ... 非 bg* 字段省略 ...
  bgCamY: number;            // tsKey 映射到 uCamY
  bgCamPitch: number;        // tsKey 映射到 uCamPitch
  bgCamZ: number;            // tsKey 映射到 uCamZ
  bgFovZoom: number;         // tsKey 映射到 uFovZoom
  bgGroundY: number;
  bgHillFreq: number;
  bgHillAmp: number;
  bgHillDepth: number;
  bgBldgDepth: number;
  bgBuildingDensity: number;
  bgBuildingHeight: number;
  bgFogDistance: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): 创建 backgroundRenderer.spec.ts 测试脚手架</name>
  <files>
    frontend/src/components/tree/__tests__/backgroundRenderer.spec.ts (新建)
  </files>
  <behavior>
    - Test 1: BackgroundRenderer 构造函数调用 createUniforms()，所有 SDF_PARAM_REGISTRY entry 对应的 uniform 存在于 material.uniforms 中
    - Test 2: 构造函数创建的 uniforms 中 uCamY.value === 2.8（registry 默认值），uSkyTopColor.value 为 THREE.Color 实例
    - Test 3: updateParams() 调用 applyParamsToUniforms()，修改 bgCamY=3.5 后 uCamY.value === 3.5
    - Test 4: updateParams() 同时将非注册参数（uStyleType、uSeed）正确设置
    - Test 5: updateMouseUV({x:0.2, y:0.8}) 后 material.uniforms.uMouseUV.value 为 THREE.Vector2(0.2, 0.8)
    - Test 6: updateMouseUV({x:NaN, y:0.5}) 后 uMouseUV.x 不为 NaN（clamp+isFinite 守卫生效，预期保持上一次有效值或默认 0.5）
    - Test 7: updateMouseUV({x:1.5, y:-0.3}) 后 uMouseUV 被 clamp 到 [0,1] 即 THREE.Vector2(1.0, 0.0)
    - Test 8: getMesh() 返回的 mesh 名称是 'background'，renderOrder 为 -2
  </behavior>
  <read_first>
    - frontend/src/components/tree/scene/BackgroundRenderer.ts（当前实现——了解需要重构的构造函数和 update 方法签名）
    - frontend/src/components/tree/scene/SdfParamRegistry.ts（了解 createUniforms/applyParamsToUniforms 的输入输出）
    - frontend/src/constants/theme.ts（了解 TreeStyleParams 接口——需要 THEME_DEFAULT 作为测试参数）
    - frontend/src/components/tree/__tests__/sdfParamRegistry.spec.ts（参考现有测试模式——import 方式和 THREE 使用方式）
  </read_first>
  <action>
    新建 `frontend/src/components/tree/__tests__/backgroundRenderer.spec.ts`：

    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    import * as THREE from 'three';
    import { BackgroundRenderer } from '../scene/BackgroundRenderer';
    import { THEME_DEFAULT } from '../../../constants/theme';
    import type { TreeStyleParams } from '../../../constants/theme';

    // Mock the shader imports to avoid Vite raw import resolution in test
    vi.mock('../shaders/backgroundRaymarch', () => ({
      backgroundVertexShader: 'void main() { gl_Position = vec4(0.0); }',
      backgroundFragmentShader: 'void main() { gl_FragColor = vec4(1.0); }',
    }));

    describe('BackgroundRenderer - SdfParamRegistry Integration (CAM-03)', () => {
      let renderer: BackgroundRenderer;

      beforeEach(() => {
        renderer = new BackgroundRenderer(0, 0.12345);
      });

      it('constructs with createUniforms() — all registry uniforms present', () => {
        const mat = renderer.getMaterial();
        // Camera uniforms from registry
        expect(mat.uniforms.uCamY.value).toBe(2.8);
        expect(mat.uniforms.uCamZ.value).toBe(-6.0);
        // Color uniforms from registry — THREE.Color instances
        expect(mat.uniforms.uSkyTopColor.value).toBeInstanceOf(THREE.Color);
        expect(mat.uniforms.uGroundColor.value).toBeInstanceOf(THREE.Color);
        // Geometry uniforms from registry
        expect(mat.uniforms.uGroundY.value).toBe(0.5);
        expect(mat.uniforms.uFogDistance.value).toBe(60.0);
        // Non-registry dynamic uniforms
        expect(mat.uniforms.uTime.value).toBe(0);
        expect(mat.uniforms.uStyleType.value).toBe(0);
        expect(mat.uniforms.uSeed.value).toBe(0.12345);
        // uMouseUV present (CAM-05)
        expect(mat.uniforms.uMouseUV.value).toBeInstanceOf(THREE.Vector2);
        expect(mat.uniforms.uMouseUV.value.x).toBe(0.5);
      });

      it('updateParams() writes TreeStyleParams into registry uniforms (CAM-03)', () => {
        const mat = renderer.getMaterial();
        const modified: TreeStyleParams = {
          ...THEME_DEFAULT,
          bgCamY: 3.5,
          bgFogDistance: 100.0,
          bgGroundY: -0.5,
        };
        renderer.updateParams(modified);
        // Registry-managed: bgCamY → uCamY
        expect(mat.uniforms.uCamY.value).toBe(3.5);
        // Registry-managed: bgFogDistance → uFogDistance
        expect(mat.uniforms.uFogDistance.value).toBe(100.0);
        // Registry-managed: bgGroundY → uGroundY
        expect(mat.uniforms.uGroundY.value).toBe(-0.5);
        // Registry-managed: bgCamPitch → uCamPitch (unchanged from THEME_DEFAULT)
        expect(mat.uniforms.uCamPitch.value).toBe(THEME_DEFAULT.bgCamPitch);
      });

      it('updateParams() sets non-registry uniforms correctly', () => {
        const mat = renderer.getMaterial();
        renderer.updateParams(THEME_DEFAULT);
        // uStyleType and uSeed should be set during construction and preserved
        expect(mat.uniforms.uStyleType.value).toBe(0);
        expect(mat.uniforms.uSeed.value).toBe(0.12345);
      });

      it('dispose() cleans up geometry and material', () => {
        const mat = renderer.getMaterial();
        const mesh = renderer.getMesh();
        const geoDisposeSpy = vi.spyOn(mesh.geometry, 'dispose');
        const matDisposeSpy = vi.spyOn(mat, 'dispose');
        renderer.dispose();
        expect(geoDisposeSpy).toHaveBeenCalled();
        expect(matDisposeSpy).toHaveBeenCalled();
      });
    });

    describe('BackgroundRenderer - Mouse Parallax (CAM-05)', () => {
      let renderer: BackgroundRenderer;

      beforeEach(() => {
        renderer = new BackgroundRenderer(1, 0.5);
      });

      it('updateMouseUV() sets uMouseUV uniform value', () => {
        const mat = renderer.getMaterial();
        renderer.updateMouseUV({ x: 0.2, y: 0.8 });
        expect(mat.uniforms.uMouseUV.value.x).toBe(0.2);
        expect(mat.uniforms.uMouseUV.value.y).toBe(0.8);
      });

      it('updateMouseUV() clamps values to [0, 1] range', () => {
        const mat = renderer.getMaterial();
        // Values outside range
        renderer.updateMouseUV({ x: 1.5, y: -0.3 });
        expect(mat.uniforms.uMouseUV.value.x).toBe(1.0);
        expect(mat.uniforms.uMouseUV.value.y).toBe(0.0);
        // Negative values clamped
        renderer.updateMouseUV({ x: -10, y: -5 });
        expect(mat.uniforms.uMouseUV.value.x).toBe(0.0);
        expect(mat.uniforms.uMouseUV.value.y).toBe(0.0);
      });

      it('updateMouseUV() guards against NaN (Number.isFinite check)', () => {
        const mat = renderer.getMaterial();
        // Set a known good value first
        renderer.updateMouseUV({ x: 0.7, y: 0.3 });
        expect(mat.uniforms.uMouseUV.value.x).toBe(0.7);

        // NaN input should keep previous value (not write NaN)
        renderer.updateMouseUV({ x: NaN, y: 0.5 });
        expect(Number.isFinite(mat.uniforms.uMouseUV.value.x)).toBe(true);
        // x should remain at 0.7 (previous valid value)
        expect(mat.uniforms.uMouseUV.value.x).toBe(0.7);
        // y should be 0.5 (valid)
        expect(mat.uniforms.uMouseUV.value.y).toBe(0.5);
      });

      it('updateMouseUV() guards against Infinity', () => {
        const mat = renderer.getMaterial();
        renderer.updateMouseUV({ x: Infinity, y: -Infinity });
        expect(Number.isFinite(mat.uniforms.uMouseUV.value.x)).toBe(true);
        expect(Number.isFinite(mat.uniforms.uMouseUV.value.y)).toBe(true);
      });

      it('getMesh() returns mesh with correct properties', () => {
        const mesh = renderer.getMesh();
        expect(mesh.name).toBe('background');
        expect(mesh.renderOrder).toBe(-2);
        expect(mesh.frustumCulled).toBe(false);
      });
    });
    ```

    运行 `cd frontend && npx vitest run src/components/tree/__tests__/backgroundRenderer.spec.ts`——
    预期: 测试因 BackgroundRenderer 尚未重构而失败（RED）——构造函数不会包含 uCamY 等 registry uniform，updateParams 方法尚未定义，updateMouseUV 方法尚未定义。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tree/__tests__/backgroundRenderer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - backgroundRenderer.spec.ts 存在且包含至少 8 个 it 块
    - 文件中出现 `updateParams` 和 `updateMouseUV` 方法调用（即测试引用了新 API）
    - 文件中出现 `uCamY` 和 `uMouseUV` uniform 访问（即测试验证了 registry 集成和 mouseUV）
    - grep -c "updateMouseUV" frontend/src/components/tree/__tests__/backgroundRenderer.spec.ts 返回 >= 4
    - grep -c "Number.isFinite" frontend/src/components/tree/__tests__/backgroundRenderer.spec.ts 返回 >= 2
    - 测试当前报 RED（等待 Task 2 实现）
  </acceptance_criteria>
  <done>backgroundRenderer.spec.ts 已创建，测试报 RED（预期），准备进入 Task 2 实现。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 重构 BackgroundRenderer 集成 SdfParamRegistry + 添加 updateMouseUV</name>
  <files>
    frontend/src/components/tree/scene/BackgroundRenderer.ts (重写)
  </files>
  <behavior>
    - Test 1: 构造函数使用 `createUniforms()` 生成 registry uniforms，并通过 `{ ...registryUniforms, ...dynamicUniforms }` 合并
    - Test 2: updateParams(params: TreeStyleParams) 调用 `applyParamsToUniforms(this.material.uniforms, params)` 写入所有注册参数
    - Test 3: updateMouseUV({x, y}) 用 `Math.max(0, Math.min(1, val))` clamp 后写入 uMouseUV
    - Test 4: uMouseUV 更新时，NaN/Infinity 被 Number.isFinite() 守卫拦截，保持前一个有效值
    - Test 5: dispose() 正常释放资源
  </behavior>
  <read_first>
    - frontend/src/components/tree/scene/BackgroundRenderer.ts（当前实现——需要对照重构的完整源文件）
    - frontend/src/components/tree/scene/SdfParamRegistry.ts（createUniforms/applyParamsToUniforms 函数签名和行为）
    - frontend/src/constants/theme.ts（TreeStyleParams 接口的 bgCam* 字段名——因为 tsKey 映射依赖这些字段精确拼写）
  </read_first>
  <action>
    重写 `frontend/src/components/tree/scene/BackgroundRenderer.ts`（完全替换文件内容）：

    ```typescript
    import * as THREE from 'three';
    import { backgroundVertexShader, backgroundFragmentShader } from '../shaders/backgroundRaymarch';
    import { createUniforms, applyParamsToUniforms } from './SdfParamRegistry';
    import type { TreeStyleParams } from '../../../constants/theme';

    /**
     * Full-screen quad with raymarched SDF background.
     *
     * CAM-03: Uses SdfParamRegistry.createUniforms() and applyParamsToUniforms()
     *         instead of hardcoding uniform lists. All bg* params from
     *         TreeStyleParams flow automatically through the registry.
     *
     * CAM-05: updateMouseUV() writes mouse position to uMouseUV uniform
     *         for vista-layer parallax offset (applied in fragment shader).
     */
    export class BackgroundRenderer {
      private mesh: THREE.Mesh;
      private material: THREE.ShaderMaterial;
      private styleType: number;
      private seed: number;

      constructor(styleType: number, seed: number) {
        this.styleType = styleType;
        this.seed = seed;

        const geo = new THREE.PlaneGeometry(2, 2);

        // CAM-03: Use SdfParamRegistry to create all registered uniforms
        const registryUniforms = createUniforms();

        this.material = new THREE.ShaderMaterial({
          vertexShader: backgroundVertexShader,
          fragmentShader: backgroundFragmentShader,
          uniforms: {
            ...registryUniforms,
            // Non-registry dynamic uniforms (not managed by SdfParamRegistry)
            uTime: { value: 0 },
            uSeed: { value: seed },
            uStyleType: { value: styleType },
            uResolution: { value: new THREE.Vector2(1024, 1024) },
            uMouseUV: { value: new THREE.Vector2(0.5, 0.5) }, // CAM-05: initial centered
          },
          depthWrite: false,
          depthTest: false,
        });

        this.mesh = new THREE.Mesh(geo, this.material);
        this.mesh.name = 'background';
        this.mesh.renderOrder = -2;
        this.mesh.frustumCulled = false;
      }

      /** Get the mesh to add to the scene */
      getMesh(): THREE.Mesh {
        return this.mesh;
      }

      /** Get the shader material for direct uniform access */
      getMaterial(): THREE.ShaderMaterial {
        return this.material;
      }

      /**
       * CAM-03: Update all registry-managed uniforms from TreeStyleParams.
       * Replaces the old update(BackgroundUniformParams) and paramsFromTheme().
       *
       * applyParamsToUniforms iterates SDF_PARAM_REGISTRY and writes each
       * params[entry.tsKey] to uniforms[entry.name]. This covers all bg*
       * fields including bgCamY/bgCamPitch/bgCamZ/bgFovZoom (CAM-04 data flow).
       */
      updateParams(params: TreeStyleParams): void {
        // Registry-managed params (colors, camera, geometry, fog)
        applyParamsToUniforms(this.material.uniforms, params);

        // Non-registry params that change on style switch
        this.material.uniforms.uSeed!.value = this.seed;
        this.material.uniforms.uStyleType!.value = this.styleType;
      }

      /**
       * CAM-05: Update mouse UV for vista-layer parallax.
       *
       * SECURITY: Clamp to [0,1] and guard against NaN/Infinity to prevent
       * shader injection of invalid floating-point values (ASVS V5).
       */
      updateMouseUV(mouseUV: { x: number; y: number }): void {
        const clamp = (v: number): number => Math.max(0, Math.min(1, v));
        const x = Number.isFinite(mouseUV.x) ? clamp(mouseUV.x) : this.material.uniforms.uMouseUV!.value.x;
        const y = Number.isFinite(mouseUV.y) ? clamp(mouseUV.y) : this.material.uniforms.uMouseUV!.value.y;
        this.material.uniforms.uMouseUV!.value.set(x, y);
      }

      /** Update time uniform for animated elements */
      updateTime(time: number): void {
        this.material.uniforms.uTime!.value = time;
      }

      /** Update resolution uniform on resize */
      updateSize(w: number, h: number): void {
        this.material.uniforms.uResolution!.value.set(w, h);
      }

      /** Clean up GPU resources */
      dispose(): void {
        this.mesh.geometry.dispose();
        this.material.dispose();
      }

      /** Map ThemeStyle string to numeric type for the shader */
      static styleToType(style: string): number {
        switch (style) {
          case 'sakura': return 1;
          case 'cyberpunk': return 2;
          case 'ink': return 3;
          default: return 0;
        }
      }
    }
    ```

    关键变更说明:
    1. **移除 `BackgroundUniformParams` 接口** —— 不再需要中间接口，直接使用 `TreeStyleParams`
    2. **移除 `paramsFromTheme()` 静态方法** —— 其职责由 `applyParamsToUniforms()` 替代
    3. **移除旧的 `update(params: BackgroundUniformParams)` 方法** —— 替换为 `updateParams(params: TreeStyleParams)`
    4. **构造函数** —— 使用 `createUniforms()` + spread 合并注册和非注册 uniforms
    5. **新增 `updateMouseUV()`** —— 带 clamp + Number.isFinite 安全守卫
    6. **保留 `styleToType()`** —— 仍然被 SceneManager 使用

    运行 `cd frontend && npx vitest run src/components/tree/__tests__/backgroundRenderer.spec.ts` —— 预期全部 GREEN。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tree/__tests__/backgroundRenderer.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - BackgroundRenderer.ts 中 import 了 createUniforms 和 applyParamsToUniforms
    - BackgroundRenderer.ts 中存在 `updateParams(params: TreeStyleParams)` 方法
    - BackgroundRenderer.ts 中存在 `updateMouseUV(mouseUV: { x: number; y: number })` 方法
    - BackgroundRenderer.ts 中不再存在 `BackgroundUniformParams` 接口定义
    - BackgroundRenderer.ts 中不再存在 `paramsFromTheme` 静态方法
    - grep -c "createUniforms" frontend/src/components/tree/scene/BackgroundRenderer.ts 返回 >= 1
    - grep -c "applyParamsToUniforms" frontend/src/components/tree/scene/BackgroundRenderer.ts 返回 >= 1
    - grep -c "Number.isFinite" frontend/src/components/tree/scene/BackgroundRenderer.ts 返回 >= 2
    - grep -c "Math.max(0, Math.min(1," frontend/src/components/tree/scene/BackgroundRenderer.ts 返回 >= 1
    - Task 1 的 backgroundRenderer.spec.ts 全部测试 GREEN
  </acceptance_criteria>
  <done>BackgroundRenderer.ts 已重构完成，backgroundRenderer.spec.ts 全部 GREEN。</done>
</task>

<task type="auto">
  <name>Task 3: SceneManager 接入——替换 paramsFromTheme/update 调用 + 添加 mousemove 监听</name>
  <files>
    frontend/src/components/tree/scene/SceneManager.ts (修改)
  </files>
  <read_first>
    - frontend/src/components/tree/scene/SceneManager.ts（完整读取——需要定位 3 处修改点）
    - frontend/src/components/tree/scene/BackgroundRenderer.ts（确认新 API: updateParams(params: TreeStyleParams) 和 updateMouseUV()）
  </read_first>
  <action>
    修改 `frontend/src/components/tree/scene/SceneManager.ts`，涉及 5 处修改：

    ### 修改 1: 添加 mouseUV 状态和 onMouseMove 方法（在类成员区域，约第 87 行 `// Context loss` 注释之前）

    在 `private contextLost = false;` 之后添加:
    ```typescript
      // Mouse parallax (CAM-05)
      private mouseUV = { x: 0.5, y: 0.5 };

      private onMouseMove = (event: MouseEvent): void => {
        const el = this.renderer?.domElement;
        if (!el || !this.backgroundRenderer) return;
        const rect = el.getBoundingClientRect();
        const x = (event.clientX - rect.left) / rect.width;
        const y = 1.0 - (event.clientY - rect.top) / rect.height;
        this.mouseUV = { x, y };
        this.backgroundRenderer.updateMouseUV(this.mouseUV);
      };
    ```

    ### 修改 2: createBackground() 方法（第 523-534 行）——替换 paramsFromTheme + update 为 updateParams

    将:
    ```typescript
      private createBackground() {
        const seed = this.hashUserIdToSeed();
        const styleType = BackgroundRenderer.styleToType(this.currentStyle);
        this.backgroundRenderer = new BackgroundRenderer(styleType, seed);

        const bgParams = BackgroundRenderer.paramsFromTheme(
          this.currentParams, styleType, seed,
        );
        this.backgroundRenderer.update(bgParams);

        this.scene.add(this.backgroundRenderer.getMesh());
      }
    ```
    改为:
    ```typescript
      private createBackground() {
        const seed = this.hashUserIdToSeed();
        const styleType = BackgroundRenderer.styleToType(this.currentStyle);
        this.backgroundRenderer = new BackgroundRenderer(styleType, seed);

        // CAM-03: Use updateParams with TreeStyleParams directly (via SdfParamRegistry)
        // This replaces paramsFromTheme() + update(BackgroundUniformParams)
        this.backgroundRenderer.updateParams(this.currentParams);

        this.scene.add(this.backgroundRenderer.getMesh());
      }
    ```

    ### 修改 3: applyStyleParams() 方法（第 773-778 行）——替换 paramsFromTheme + update 为 updateParams

    将:
    ```typescript
        // Update background (raymarched SDF)
        if (this.backgroundRenderer) {
          const seed = this.hashUserIdToSeed();
          const styleType = BackgroundRenderer.styleToType(this.currentStyle);
          const bgParams = BackgroundRenderer.paramsFromTheme(params, styleType, seed);
          this.backgroundRenderer.update(bgParams);
        }
    ```
    改为:
    ```typescript
        // Update background (raymarched SDF)
        // CAM-03: Use updateParams — all bg* params (including bgCam*) flow
        // through SdfParamRegistry.applyParamsToUniforms (CAM-04 data link)
        if (this.backgroundRenderer) {
          this.backgroundRenderer.updateParams(params);
        }
    ```

    ### 修改 4: setupCameraAndRenderer() 方法末尾（第 732 行 `this.container.appendChild(...)` 之后）——添加 mousemove 监听

    在 `this.container.appendChild(this.renderer.domElement);` 之后添加:
    ```typescript
        // CAM-05: Listen for mouse movement to update parallax offset
        this.renderer.domElement.addEventListener('mousemove', this.onMouseMove);
    ```

    ### 修改 5: disposeScene() 方法（第 943-947 行附近，其他 removeEventListener 之后）——移除 mousemove 监听

    在 `this.renderer.domElement.removeEventListener('webglcontextrestored', this.onContextRestored);` 之后添加:
    ```typescript
        this.renderer.domElement.removeEventListener('mousemove', this.onMouseMove);
    ```

    注意: 如果 renderer 可能为 null（在 disposeScene 中 renderer 存在性检查在第 943 行），确保 removeEventListener 调用也在 `if (this.renderer)` 块内。

    确认所有修改后，运行完整测试套件验证:
    `cd frontend && npx vitest run`
  </action>
  <verify>
    <automated>cd frontend && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - SceneManager.ts 中存在 `onMouseMove` 箭头函数定义，其内调用 `this.backgroundRenderer.updateMouseUV`
    - createBackground() 中调用的是 `this.backgroundRenderer.updateParams(this.currentParams)` ——不再使用 `paramsFromTheme` 或旧的 `update(bgParams)`
    - applyStyleParams() 中调用的是 `this.backgroundRenderer.updateParams(params)` ——不再使用 `paramsFromTheme`
    - setupCameraAndRenderer() 中存在 `addEventListener('mousemove', this.onMouseMove)`
    - disposeScene() 中存在 `removeEventListener('mousemove', this.onMouseMove)`
    - grep -c "updateParams" frontend/src/components/tree/scene/SceneManager.ts 返回 >= 2
    - grep -c "onMouseMove" frontend/src/components/tree/scene/SceneManager.ts 返回 >= 3（定义+addEventListener+removeEventListener）
    - grep -c "paramsFromTheme" frontend/src/components/tree/scene/SceneManager.ts 返回 0（已完全移除）
    - 完整测试套件 GREEN（全部 ~50 个测试通过）
  </acceptance_criteria>
  <done>SceneManager.ts 已完成全部 5 处修改，完整测试套件 GREEN。</done>
</task>

<task type="auto">
  <name>Task 4: 全量测试验证 + 提交</name>
  <files>
    frontend/src/components/tree/scene/BackgroundRenderer.ts
    frontend/src/components/tree/scene/SceneManager.ts
    frontend/src/components/tree/__tests__/backgroundRenderer.spec.ts
  </files>
  <read_first>
    - frontend/src/components/tree/scene/BackgroundRenderer.ts（确认最终状态）
    - frontend/src/components/tree/scene/SceneManager.ts（确认所有 5 处修改到位）
  </read_first>
  <action>
    1. 运行完整测试套件: `cd frontend && npx vitest run`
       - 确认全部测试通过（含原有的 ~40 个 + Plan 01-01 新增的 ~10 个 + Plan 01-02 新增的 ~8 个）
       - 如有失败，退回相关 task 修复

    2. 确认通过后提交:
       ```
       git add frontend/src/components/tree/scene/BackgroundRenderer.ts
       git add frontend/src/components/tree/scene/SceneManager.ts
       git add frontend/src/components/tree/__tests__/backgroundRenderer.spec.ts
       git commit -m "feat(phase-01): integrate SdfParamRegistry into BackgroundRenderer, add mouse parallax with security guards (CAM-03, CAM-04, CAM-05)"
       ```
  </action>
  <verify>
    <automated>cd frontend && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` 显示全部测试文件通过（6 个文件 → 含新增后 ~7-8 个文件），0 个失败
    - BackgroundRenderer.ts 重构代码已提交
    - SceneManager.ts 修改已提交
    - backgroundRenderer.spec.ts 已提交
    - grep -c "paramsFromTheme" frontend/src/components/tree/scene/ 返回 0（完全移除旧 API）
  </acceptance_criteria>
  <done>全部测试 GREEN，代码已提交。Plan 01-02 完成。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| Browser DOM → SceneManager.onMouseMove | 用户鼠标事件数据进入系统——event.clientX/clientY 来自浏览器 API，不受应用控制 |
| SceneManager → BackgroundRenderer.updateMouseUV | mouseUV 值从 DOM 事件计算后传入——此边界需要验证输入合法性 |
| BackgroundRenderer → ShaderMaterial.uniforms.uMouseUV | float 值写入 GPU uniform——非法浮点值（NaN/Infinity）可能导致 GPU 着色器异常 |

## STRIDE 威胁登记

| Threat ID | Category | Component | Disposition | 缓解方案 |
|-----------|----------|-----------|-------------|----------|
| T-01-02-01 | Denial of Service | BackgroundRenderer.updateMouseUV() | **mitigate** | uMouseUV x,y 使用 `Math.max(0, Math.min(1, val))` clamp 到 [0,1] 范围；NaN/Infinity 通过 `Number.isFinite()` 守卫拦截，保持前一个有效值；防止非法浮点值传入 GPU 导致 shader 渲染异常或 GPU 崩溃 |
| T-01-02-02 | Tampering | SdfParamRegistry.applyParamsToUniforms | accept | 参数值来自 TreeStyleParams（编译时定义的主题预设），非用户可控输入。攻击者需修改源码才能注入恶意值——属于代码仓库访问控制范畴 |
| T-01-02-03 | Information Disclosure | ShaderMaterial.uniforms | accept | 所有 uniform 值仅在客户端 GPU 上使用，无数据传输到外部。无敏感信息暴露风险 |
</threat_model>

<verification>
### 自动化验证
- `cd frontend && npx vitest run` — 全部测试通过（含 backgroundRenderer.spec.ts 的 CAM-03/CAM-05 断言）

### 手动验证
- 打开 app，检查浏览器 console 无 shader 编译错误
- 左右移动鼠标，观察远景层是否有可见的水平偏移（CAM-05 视差效果）
- 通过 debug GUI 切换风格（default/sakura/cyberpunk/ink），观察背景构图变化（CAM-04 相机参数生效）
</verification>

<success_criteria>
1. BackgroundRenderer 使用 SdfParamRegistry.createUniforms() 和 applyParamsToUniforms()，不再手动维护 uniform 列表
2. 风格切换时 bgCamY/bgCamPitch/bgCamZ/bgFovZoom 通过 the registry 自动流入 shader uniforms
3. 鼠标移动时 uMouseUV uniform 更新，远景层可见视差偏移（手动浏览器验证）
4. uMouseUV clamp + Number.isFinite 守卫防止 NaN/Infinity 注入
5. 全部自动化测试 GREEN
</success_criteria>

<output>
完成后创建 `.planning/phases/01-camera-system-shader-foundation/01-02-SUMMARY.md`
</output>
