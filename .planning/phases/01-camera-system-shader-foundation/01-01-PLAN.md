---
phase: 01-camera-system-shader-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/tree/shaders/vista/mapDefault.glsl
  - frontend/src/components/tree/shaders/vista/mapSakura.glsl
  - frontend/src/components/tree/shaders/vista/mapCyberpunk.glsl
  - frontend/src/components/tree/shaders/vista/mapInk.glsl
  - frontend/src/components/tree/shaders/backgroundRaymarch.ts
  - frontend/src/components/tree/__tests__/vistaMaps.spec.ts
  - frontend/src/components/tree/__tests__/backgroundLayers.spec.ts
  - frontend/src/constants/__tests__/theme.spec.ts
autonomous: true
requirements: [ARCH-01, CAM-01, CAM-02]

must_haves:
  truths:
    - "Shader 源码包含 uCamY/uCamPitch/uCamZ/uFovZoom 四个 camera uniform 声明（由 generateGlslUniforms() 自动生成）"
    - "Shader 源码中不再存在硬编码的 ro = vec3(0.0, 2.8, -6.0)、lookAt = vec3(0.0, 3.5, 30.0)、zoom = 1.8"
    - "相机 forward 方向使用 sin(uCamPitch) 计算，pitch 为负值时 sin < 0 确保向下俯视（CAM-02）"
    - "四个 vista map 函数（mapDefault/mapSakura/mapCyberpunk/mapInk）分别存在于独立 .glsl 文件中"
    - "组合后的 fragmentShader 通过模板字面量插值包含全部 4 个 map 函数"
    - "全部 4 个主题预设的 bgCamPitch 均为负值"
  artifacts:
    - path: "frontend/src/components/tree/shaders/vista/mapDefault.glsl"
      provides: "mapDefault 函数——默认风格的山丘+散落树木"
      contains: "float mapDefault(vec3 p)"
    - path: "frontend/src/components/tree/shaders/vista/mapSakura.glsl"
      provides: "mapSakura 函数——樱花风格的鸟居+寺庙+町屋"
      contains: "float mapSakura(vec3 p)"
    - path: "frontend/src/components/tree/shaders/vista/mapCyberpunk.glsl"
      provides: "mapCyberpunk 函数——赛博朋克风格的摩天楼城市"
      contains: "float mapCyberpunk(vec3 p)"
    - path: "frontend/src/components/tree/shaders/vista/mapInk.glsl"
      provides: "mapInk 函数——水墨风格的分层远山"
      contains: "float mapInk(vec3 p)"
    - path: "frontend/src/components/tree/shaders/backgroundRaymarch.ts"
      provides: "重组后的 shader 组装文件——Vite ?raw 导入各 .glsl 文件，模板字面量拼接，相机计算改为 uniform 驱动"
      exports: ["backgroundVertexShader", "backgroundFragmentShader"]
      min_lines: 120
    - path: "frontend/src/components/tree/__tests__/vistaMaps.spec.ts"
      provides: "ARCH-01 自动化测试——验证 .glsl 文件可导入且包含正确的 map 函数"
    - path: "frontend/src/components/tree/__tests__/backgroundLayers.spec.ts"
      provides: "CAM-01 自动化测试扩展——验证 shader 中无硬编码相机值"
    - path: "frontend/src/constants/__tests__/theme.spec.ts"
      provides: "CAM-02 自动化测试扩展——验证所有主题 bgCamPitch 为负"
  key_links:
    - from: "backgroundRaymarch.ts"
      to: "vista/mapDefault.glsl"
      via: "import mapDefault from './vista/mapDefault.glsl?raw'"
      pattern: "import mapDefault from.*\\.glsl\\?raw"
    - from: "backgroundRaymarch.ts"
      to: "generateGlslUniforms()"
      via: "模板字面量 ${generateGlslUniforms()}"
      pattern: "generateGlslUniforms\\(\\)"
    - from: "main() 入口"
      to: "uCamY/uCamPitch/uCamZ/uFovZoom"
      via: "ro = vec3(0.0, uCamY, uCamZ); forward.y = sin(uCamPitch)"
      pattern: "ro\\s*=\\s*vec3\\(0\\.0,\\s*uCamY"
---

<objective>
**任务:** 重构 SDF 背景 shader——将 4 个 vista map 函数提取为独立 .glsl 文件（ARCH-01），替换硬编码相机值为 uniform 驱动（CAM-01），确保相机向下俯视（CAM-02）。

**目的:** 使得 shader 的相机参数可通过 TreeStyleParams 动态控制，每个 map 函数独立文件化便于后续 LLM 生成和独立编辑。

**产出:** 4 个新的 .glsl 文件、重组后的 backgroundRaymarch.ts、扩展的测试文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/components/tree/shaders/backgroundRaymarch.ts
@frontend/src/components/tree/scene/SdfParamRegistry.ts
@frontend/src/constants/theme.ts
@frontend/src/components/tree/__tests__/backgroundLayers.spec.ts
@frontend/src/constants/__tests__/theme.spec.ts
@frontend/src/components/tree/shaders/sdfPrimitives.ts
@frontend/src/components/tree/shaders/sdfArchitecture.ts

<interfaces>
<!-- 关键接口——执行器直接使用，无需代码库探索 -->

来自 SdfParamRegistry.ts:
```typescript
// generateGlslUniforms() 输出示例（基于当前 SDF_PARAM_REGISTRY 中 15 个 entry）：
// uniform vec3 uSkyTopColor; // rgb
// uniform vec3 uSkyBottomColor; // rgb
// uniform vec3 uGroundColor; // rgb
// uniform float uCamY;
// uniform float uCamPitch;
// uniform float uCamZ;
// uniform float uFovZoom;
// uniform float uGroundY;
// uniform float uHillFreq;
// uniform float uHillAmp;
// uniform float uHillDepth;
// uniform float uBldgDepth;
// uniform float uBuildingDensity;
// uniform float uBuildingHeight;
// uniform float uFogDistance;

export function generateGlslUniforms(): string;
```

来自 backgroundRaymarch.ts 当前结构:
```glsl
// 当前: 硬编码手动声明 uniforms（第 17-28 行）
uniform vec3 uSkyTopColor;
uniform vec3 uSkyBottomColor;
// ... 等 11 个手动声明 ...

// 当前: 4 个 map 函数内联在文件中（第 34-163 行）
float mapDefault(vec3 p) { ... }
float mapSakura(vec3 p) { ... }
float mapCyberpunk(vec3 p) { ... }
float mapInk(vec3 p) { ... }

// 当前: main() 中的硬编码相机（第 220-232 行）
vec3 ro = vec3(0.0, 2.8, -6.0);
vec3 lookAt = vec3(0.0, 3.5, 30.0);
vec3 forward = normalize(lookAt - ro);
// ...
float zoom = 1.8;
```

TreeStyleParams 中 bgCam* 字段（theme.ts）:
```typescript
// 每个主题预设均定义了以下 bg* 值
bgCamY: number;       // 默认 2.8, sakura 3.2, cyberpunk 2.2, ink 3.5
bgCamPitch: number;   // 默认 -0.15, sakura -0.18, cyberpunk -0.12, ink -0.2
bgCamZ: number;       // 默认 -6.0, sakura -5.5, cyberpunk -7.0, ink -8.0
bgFovZoom: number;    // 默认 1.8, sakura 1.6, cyberpunk 2.0, ink 2.2
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): 创建测试脚手架——vistaMaps.spec.ts + 扩展现有测试</name>
  <files>
    frontend/src/components/tree/__tests__/vistaMaps.spec.ts (新建)
    frontend/src/components/tree/__tests__/backgroundLayers.spec.ts (扩展)
    frontend/src/constants/__tests__/theme.spec.ts (扩展)
  </files>
  <behavior>
    - Test 1 (vistaMaps.spec.ts): 每个 .glsl 文件可通过 Vite ?raw 导入为字符串，且字符串非空、长度 > 200
    - Test 2 (vistaMaps.spec.ts): 每个 .glsl 文件内容包含且仅包含一个 map 函数定义（如 mapDefault.glsl 包含 `float mapDefault(vec3 p)`）
    - Test 3 (vistaMaps.spec.ts): 组合后的 backgroundFragmentShader 包含全部 4 个 map 函数名（mapDefault/mapSakura/mapCyberpunk/mapInk）
    - Test 4 (backgroundLayers.spec.ts 新增): backgroundFragmentShader 不包含硬编码 `ro = vec3(0.0, 2.8`、不包含 `lookAt = vec3(0.0, 3.5`、不包含独立的 `zoom = 1.8`（不再有非 uniform 的 zoom 赋值）
    - Test 5 (backgroundLayers.spec.ts 新增): backgroundFragmentShader 包含 `uniform float uCamY;` 和 `uniform float uCamPitch;` 声明
    - Test 6 (theme.spec.ts 新增): 全部 4 个主题预设（default/sakura/cyberpunk/ink）的 bgCamPitch 值 < 0
    - Test 7 (theme.spec.ts 新增): Math.sin(bgCamPitch) < 0 对所有预设成立（确认 sin(负值) < 0，向下俯视方向正确）
  </behavior>
  <read_first>
    - frontend/src/components/tree/shaders/backgroundRaymarch.ts（了解当前 shader 结构和 map 函数签名）
    - frontend/src/components/tree/scene/SdfParamRegistry.ts（了解 generateGlslUniforms 输出格式）
    - frontend/src/constants/theme.ts（了解 THEME_PRESETS 中 bgCamPitch 值）
    - frontend/src/components/tree/__tests__/backgroundLayers.spec.ts（了解现有测试模式）
    - frontend/src/constants/__tests__/theme.spec.ts（了解现有测试模式）
  </read_first>
  <action>
    1. 新建 `frontend/src/components/tree/__tests__/vistaMaps.spec.ts`：
       - 使用 `import.meta.glob('../shaders/vista/*.glsl', { query: '?raw', import: 'default', eager: true })` 验证所有 .glsl 文件可导入
       - 对每个导入的文件内容断言: 非空字符串、长度 > 200、包含对应的 `float map*(vec3 p)` 签名
       - 导入 `backgroundFragmentShader` 并断言包含全部 4 个 map 函数名

    2. 扩展 `frontend/src/components/tree/__tests__/backgroundLayers.spec.ts`：
       - 在 `SDF-01` describe 块中新增 `describe('CAM-01: No hardcoded camera values')` 块
       - 添加测试: `expect(backgroundFragmentShader).not.toMatch(/ro\s*=\s*vec3\s*\(\s*0\.0\s*,\s*2\.8/)` → 无硬编码 ro 的 x=0,y=2.8,z=-6
       - 添加测试: `expect(backgroundFragmentShader).not.toMatch(/lookAt\s*=\s*vec3\s*\(\s*0\.0\s*,\s*3\.5/)` → 无硬编码 lookAt 的 x=0,y=3.5
       - 添加测试: `expect(backgroundFragmentShader).not.toMatch(/zoom\s*=\s*1\.8/)` → 无硬编码 zoom=1.8
       - 添加测试: 验证 `backgroundFragmentShader` 包含 `uniform float uCamY;` 和 `uniform float uCamPitch;`

    3. 扩展 `frontend/src/constants/__tests__/theme.spec.ts`：
       - 在 `Theme Presets (SDF-03)` describe 块中新增 `describe('CAM-02: Camera pitch is negative (downward look)')` 块
       - 遍历 4 个主题名: 断言 `THEME_PRESETS[name].bgCamPitch < 0`
       - 添加测试: 对每个预设，`Math.sin(THEME_PRESETS[name].bgCamPitch) < 0` 为 true（确认 sin(负角度) 产生向下的 forward.y 分量）

    4. 运行 `cd frontend && npx vitest run`——预期: 新建的 vistaMaps.spec.ts 因为 .glsl 文件尚不存在而失败（RED），扩展的测试因 shader 仍有硬编码值而失败（RED）。共计至少 2 个测试文件报红。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tree/__tests__/vistaMaps.spec.ts src/components/tree/__tests__/backgroundLayers.spec.ts src/constants/__tests__/theme.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - vistaMaps.spec.ts 存在且包含至少 3 个测试用例（it 块）
    - backgroundLayers.spec.ts 中存在 `CAM-01` 或 `No hardcoded camera` describe 块
    - theme.spec.ts 中存在 `CAM-02` 或 `pitch is negative` describe 块
    - grep -c "bgCamPitch.*<.*0" frontend/src/constants/__tests__/theme.spec.ts 返回 >= 1
    - 新建/扩展的测试暂时失败（RED）——等待 Task 2 实现后变绿
  </acceptance_criteria>
  <done>3 个测试文件均已创建/扩展，vitest 运行时新测试报 RED（预期行为），准备进入 Task 2 实现阶段。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 提取 map 函数为 .glsl + 重组 shader 组装 + 替换相机计算</name>
  <files>
    frontend/src/components/tree/shaders/vista/mapDefault.glsl (新建)
    frontend/src/components/tree/shaders/vista/mapSakura.glsl (新建)
    frontend/src/components/tree/shaders/vista/mapCyberpunk.glsl (新建)
    frontend/src/components/tree/shaders/vista/mapInk.glsl (新建)
    frontend/src/components/tree/shaders/backgroundRaymarch.ts (修改)
  </files>
  <behavior>
    - Test 1: 4 个 .glsl 文件可被 Vite `?raw` 导入为字符串
    - Test 2: 每个 .glsl 文件内容以 `float map*(vec3 p) {` 开头，包含完整的 SDF 函数体
    - Test 3: backgroundFragmentShader 通过模板字面量 `${mapDefault}${mapSakura}${mapCyberpunk}${mapInk}` 组合所有 map 函数
    - Test 4: backgroundFragmentShader 包含 `${generateGlslUniforms()}` 生成的 uniform 声明
    - Test 5: main() 中 `ro = vec3(0.0, uCamY, uCamZ)` 且 `forward.y = sin(uCamPitch)` 且 `rd` 缩放使用 `uFovZoom`
    - Test 6: shader 不再包含任何硬编码的 ro/lookAt/zoom 字面量值
  </behavior>
  <read_first>
    - frontend/src/components/tree/shaders/backgroundRaymarch.ts（当前 shader 完整源码——需要从中提取 map 函数体）
    - frontend/src/components/tree/scene/SdfParamRegistry.ts（确认 generateGlslUniforms() 输出格式和内容）
    - frontend/src/components/tree/shaders/sdfPrimitives.ts（了解 SDF_PRIMITIVES export 格式）
    - frontend/src/components/tree/shaders/sdfArchitecture.ts（了解 SDF_ARCHITECTURE export 格式）
  </read_first>
  <action>
    ### 步骤 1: 创建 vista/ 目录并写入 4 个 .glsl 文件

    在 `frontend/src/components/tree/shaders/vista/` 下创建 4 个纯 GLSL 文件（无 TypeScript、无 export、无模板字面量标记）：

    **mapDefault.glsl** — 从 backgroundRaymarch.ts 第 35-60 行提取 mapDefault 函数体：
    ```glsl
    // Default style: hills + scattered trees
    float mapDefault(vec3 p) {
      float ground = sdPlane(p, vec3(0.0, 1.0, 0.0), uGroundY);

      // Rolling hills in the distance
      float hill1 = sdHill(
        p - vec3(-10.0, -5.5, 40.0),
        6.0 + hash11(uSeed + 1.0) * 4.0,
        18.0 + hash11(uSeed + 2.0) * 5.0
      );
      float hill2 = sdHill(
        p - vec3(15.0, -5.0, 50.0),
        4.5 + hash11(uSeed + 3.0) * 3.0,
        15.0 + hash11(uSeed + 4.0) * 4.0
      );
      float hill3 = sdHill(
        p - vec3(0.0, -4.5, 60.0),
        3.0 + hash11(uSeed + 5.0) * 2.5,
        20.0 + hash11(uSeed + 6.0) * 6.0
      );

      float d = ground;
      d = opS(d, hill1, 2.0);
      d = opS(d, hill2, 2.0);
      d = opS(d, hill3, 2.5);
      return d;
    }
    ```

    **mapSakura.glsl** — 从 backgroundRaymarch.ts 第 63-99 行提取 mapSakura 函数体
    **mapCyberpunk.glsl** — 从 backgroundRaymarch.ts 第 102-135 行提取 mapCyberpunk 函数体
    **mapInk.glsl** — 从 backgroundRaymarch.ts 第 138-163 行提取 mapInk 函数体

    每个文件仅包含: GLSL 注释行 + `float map*(vec3 p) {` ... 函数体 ... `}`。不包含 TypeScript 语法。

    ### 步骤 2: 重组 backgroundRaymarch.ts

    修改 `frontend/src/components/tree/shaders/backgroundRaymarch.ts`：

    **2a. 添加 .glsl 文件导入（文件顶部，在现有 import 之后）：**
    ```typescript
    import mapDefault from './vista/mapDefault.glsl?raw';
    import mapSakura from './vista/mapSakura.glsl?raw';
    import mapCyberpunk from './vista/mapCyberpunk.glsl?raw';
    import mapInk from './vista/mapInk.glsl?raw';
    import { generateGlslUniforms } from '../scene/SdfParamRegistry';
    ```

    **2b. 重构 backgroundFragmentShader 模板字面量：**
    - 保留 `${SDF_PRIMITIVES}` 和 `${SDF_ARCHITECTURE}` 在顶部（不变）
    - 紧接其后插入 4 个 map 函数: `${mapDefault}${mapSakura}${mapCyberpunk}${mapInk}`
    - 替换第 17-28 行的全部手动 uniform 声明为 `${generateGlslUniforms()}`
    - 保留非注册动态 uniforms 的手动声明: `uniform float uTime;` `uniform float uSeed;` `uniform float uStyleType;` `uniform vec2 uResolution;` ——这些不在 registry 中
    - 新增: `uniform vec2 uMouseUV;` （CAM-05 预留——Plan 01-02 将在 JS 侧提供数据）

    **2c. 删除内联的 map 函数定义：**
    - 从 fragmentShader 中删除第 34-163 行的 mapDefault/mapSakura/mapCyberpunk/mapInk 函数定义

    **2d. 替换 main() 中的相机计算——这是 CAM-01 + CAM-02 的核心修改：**
    - 删除第 220-232 行的硬编码相机代码：
      ```glsl
      // 删除:
      vec3 ro = vec3(0.0, 2.8, -6.0);
      vec3 lookAt = vec3(0.0, 3.5, 30.0);
      vec3 forward = normalize(lookAt - ro);
      // ... worldUp/right/up ...
      float zoom = 1.8;
      vec3 rd = normalize(forward + right * uv.x * zoom + up * uv.y * zoom);
      ```
    - 替换为 uniform 驱动相机:
      ```glsl
      // Camera position from uniforms
      vec3 ro = vec3(0.0, uCamY, uCamZ);

      // Forward direction from pitch (yaw fixed at 0)
      // uCamPitch < 0 → sin(uCamPitch) < 0 → forward.y < 0 → looking downward (CAM-02)
      vec3 forward = normalize(vec3(0.0, sin(uCamPitch), cos(uCamPitch)));

      // Camera basis vectors
      vec3 worldUp = vec3(0.0, 1.0, 0.0);
      vec3 right = normalize(cross(forward, worldUp));
      vec3 up = cross(right, forward);

      // Ray direction with FOV zoom
      vec3 rd = normalize(forward + right * uv.x * uFovZoom + up * uv.y * uFovZoom);
      ```

    ### 步骤 3: 验证 shader 结构正确性

    确保重组后的 fragmentShader 结构为:
    ```
    ${SDF_PRIMITIVES}
    ${SDF_ARCHITECTURE}
    ${mapDefault}
    ${mapSakura}
    ${mapCyberpunk}
    ${mapInk}
    ${generateGlslUniforms()}
    uniform float uTime;          // 动态 uniform，非 registry 管理
    uniform float uSeed;
    uniform float uStyleType;
    uniform vec2 uResolution;
    uniform vec2 uMouseUV;
    varying vec2 vScreenUV;
    // ... map() dispatch, calcNormal, applyToonLighting, softShadow, main() ...
    ```

    ### 步骤 4: 运行测试确认 GREEN

    Task 1 中创建的测试现在应该全部通过:
    - vistaMaps.spec.ts: .glsl 文件可导入
    - backgroundLayers.spec.ts: 无硬编码相机值，包含 camera uniform 声明
    - theme.spec.ts: 所有 bgCamPitch 为负（此测试本来就该通过，数据未变）
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/tree/__tests__/vistaMaps.spec.ts src/components/tree/__tests__/backgroundLayers.spec.ts src/constants/__tests__/theme.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 4 个 .glsl 文件存在于 `frontend/src/components/tree/shaders/vista/` 目录
    - 每个 .glsl 文件内容以 `float map*(vec3 p)` 开头，无 TypeScript 语法
    - grep -c "import mapDefault from.*glsl?raw" frontend/src/components/tree/shaders/backgroundRaymarch.ts 返回 1
    - grep -c "generateGlslUniforms" frontend/src/components/tree/shaders/backgroundRaymarch.ts 返回 >= 1
    - grep -c "ro\s*=\s*vec3\s*(\s*0\.0\s*,\s*uCamY\s*,\s*uCamZ" frontend/src/components/tree/shaders/backgroundRaymarch.ts 返回 >= 1 (用宽松正则)
    - grep -c "sin(uCamPitch)" frontend/src/components/tree/shaders/backgroundRaymarch.ts 返回 >= 1
    - grep -c "uFovZoom" frontend/src/components/tree/shaders/backgroundRaymarch.ts 返回 >= 2 (一次声明+一次使用)
    - 任务 1 的全部测试变绿（GREEN）
    - 原有的全部 40 个测试保持通过（cd frontend && npx vitest run）
  </acceptance_criteria>
  <done>4 个 .glsl 文件已创建，backgroundRaymarch.ts 重组完成，全部测试 GREEN。</done>
</task>

<task type="auto">
  <name>Task 3: 全量测试验证 + 提交</name>
  <files>
    frontend/src/components/tree/shaders/vista/mapDefault.glsl
    frontend/src/components/tree/shaders/vista/mapSakura.glsl
    frontend/src/components/tree/shaders/vista/mapCyberpunk.glsl
    frontend/src/components/tree/shaders/vista/mapInk.glsl
    frontend/src/components/tree/shaders/backgroundRaymarch.ts
    frontend/src/components/tree/__tests__/vistaMaps.spec.ts
    frontend/src/components/tree/__tests__/backgroundLayers.spec.ts
    frontend/src/constants/__tests__/theme.spec.ts
  </files>
  <read_first>
    - frontend/src/components/tree/shaders/vista/mapDefault.glsl（确认内容正确）
    - frontend/src/components/tree/shaders/backgroundRaymarch.ts（确认重组结构正确）
  </read_first>
  <action>
    1. 运行完整测试套件: `cd frontend && npx vitest run`
       - 确认全部测试通过（含原有的 40 个 + 本次新增的约 10 个）
       - 如有失败，退回 Task 2 修复

    2. 确认通过后提交:
       ```
       git add frontend/src/components/tree/shaders/vista/
       git add frontend/src/components/tree/shaders/backgroundRaymarch.ts
       git add frontend/src/components/tree/__tests__/vistaMaps.spec.ts
       git add frontend/src/components/tree/__tests__/backgroundLayers.spec.ts
       git add frontend/src/constants/__tests__/theme.spec.ts
       git commit -m "feat(phase-01): extract vista map functions to .glsl files, replace hardcoded camera with uniform-driven 4-parameter model (ARCH-01, CAM-01, CAM-02)"
       ```
  </action>
  <verify>
    <automated>cd frontend && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` 显示全部测试文件通过，0 个失败
    - 4 个 .glsl 文件已提交到 git
    - backgroundRaymarch.ts 修改已提交
    - 3 个测试文件（新建或扩展）已提交
  </acceptance_criteria>
  <done>全部测试 GREEN，代码已提交。Plan 01-01 完成。</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| 无 | Plan 01-01 仅修改客户端 shader 源码和测试文件。无运行时用户输入、无网络边界、无数据持久化。 |

## STRIDE 威胁登记

| Threat ID | Category | Component | Disposition | 缓解方案 |
|-----------|----------|-----------|-------------|----------|
| T-01-01-01 | Tampering | .glsl 文件源码 | accept | Vite 构建时静态导入，攻击者需直接修改源码仓库——属于代码仓库访问控制范畴，非应用层安全 |
| T-01-01-02 | Denial of Service | Fragment Shader 编译 | accept | 若重构后 shader 编译失败，已有 ERR-01 降级方案（回退 sky2d.ts）——但此威胁属 Phase 8 管辖，Phase 1 不做额外缓解 |
</threat_model>

<verification>
### 自动化验证
- `cd frontend && npx vitest run` — 全部测试通过（含新增的 CAM-01/CAM-02/ARCH-01 断言）

### 手动验证
- 检查 `frontend/src/components/tree/shaders/vista/` 目录是否包含 4 个 .glsl 文件
- 检查 backgroundRaymarch.ts 中是否使用了 `import ... from '...?raw'` 和 `${generateGlslUniforms()}`
- 检查 main() 中 `ro = vec3(0.0, uCamY, uCamZ)` 和 `sin(uCamPitch)` 是否存在
</verification>

<success_criteria>
1. 4 个 vista map 函数分别存在于独立 .glsl 文件中，每个文件为纯 GLSL（无 TypeScript）
2. backgroundRaymarch.ts 通过 Vite `?raw` 导入组合所有 map 函数 + `${generateGlslUniforms()}` 生成 uniform 声明
3. shader 中不再存在硬编码的 `ro = vec3(0.0, 2.8, -6.0)` / `lookAt = vec3(0.0, 3.5, 30.0)` / `zoom = 1.8`
4. 相机计算改为 `ro = vec3(0.0, uCamY, uCamZ)` 且 `forward.y = sin(uCamPitch)` 确保负 pitch → 向下俯视
5. 全部自动化测试 GREEN（原有 40 个 + 新增 ~10 个）
</success_criteria>

<output>
完成后创建 `.planning/phases/01-camera-system-shader-foundation/01-01-SUMMARY.md`
</output>
