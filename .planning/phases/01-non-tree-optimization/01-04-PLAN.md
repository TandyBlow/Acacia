---
phase: 01-non-tree-optimization
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/src/stores/authStore.ts
autonomous: true
requirements:
  - NF-10

must_haves:
  truths:
    - "No console.warn output from authStore.ts in production"
  artifacts:
    - path: "frontend/src/stores/authStore.ts"
      provides: "Auth store without debug console.warn lines"
      contains: "async function initialize"
      contains: "async function submitByKnob"
  key_links: []
---

<objective>
Remove 4 debug `console.warn()` calls from `frontend/src/stores/authStore.ts` that were left in from a debugging session. These lines log user authentication state (including JWT payload contents) to the browser console in production.

Purpose: Clean production console output and prevent potential exposure of user authentication state in browser developer tools.

Output: `frontend/src/stores/authStore.ts` with 4 lines deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
The current state of the 4 lines to remove in authStore.ts:

Line 99: `      console.warn('[authStore] initialize() result:', JSON.stringify(currentUser));`
  - Inside `initialize()`, after `const currentUser = await authAdapter.initialize();`
  - Remove the entire console.warn line. Keep the `assignUser(currentUser);` line that follows.

Line 101: `      console.warn('[authStore] after assignUser, currentUsername:', currentUsername.value, 'user:', JSON.stringify(user.value));`
  - Inside `initialize()`, after `assignUser(currentUser);`
  - Remove the entire console.warn line. Keep the closing brace of the try block.

Line 107: `      console.warn('[authStore] onAuthStateChange:', JSON.stringify(nextUser), 'current user:', JSON.stringify(user.value));`
  - Inside `initialize()`, inside the `authAdapter.onAuthStateChange()` callback
  - Remove the entire console.warn line. Keep the `assignUser(nextUser);` line that follows.

Line 109: `      console.warn('[authStore] after onAuthStateChange assignUser, currentUsername:', currentUsername.value);`
  - Inside `initialize()`, inside the callback, after `assignUser(nextUser);`
  - Remove the entire console.warn line. Keep the `if (nextUser) {` block that follows.

All 4 lines are in the `initialize()` function (lines 91-116). No other lines in authStore.ts should change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove console.warn debug lines from authStore.ts</name>
  <files>frontend/src/stores/authStore.ts</files>
  <read_first>
    - frontend/src/stores/authStore.ts (lines 91-116, the initialize() function)
  </read_first>
  <action>
Remove exactly 4 lines from `frontend/src/stores/authStore.ts`:

1. **Line 99:** `console.warn('[authStore] initialize() result:', JSON.stringify(currentUser));`
2. **Line 101:** `console.warn('[authStore] after assignUser, currentUsername:', currentUsername.value, 'user:', JSON.stringify(user.value));`
3. **Line 107:** `console.warn('[authStore] onAuthStateChange:', JSON.stringify(nextUser), 'current user:', JSON.stringify(user.value));`
4. **Line 109:** `console.warn('[authStore] after onAuthStateChange assignUser, currentUsername:', currentUsername.value);`

All 4 lines are inside the `initialize()` function.

**After removal, the initialize() function should look like:**
```typescript
async function initialize(): Promise<void> {
    if (initialized.value || !authAdapter) {
      initialized.value = true;
      return;
    }

    try {
      const currentUser = await authAdapter.initialize();
      assignUser(currentUser);
    } catch (error) {
      errorMessage.value = formatAuthError(error);
    }

    authAdapter.onAuthStateChange((nextUser) => {
      assignUser(nextUser);
      if (nextUser) {
        errorMessage.value = null;
      }
    });

    initialized.value = true;
  }
```

**Do NOT change:**
- The try/catch structure
- The `assignUser()` calls
- The `onAuthStateChange` callback logic
- Any other function or line in the file
- The `formatAuthError` function
- The `clearSecretsAfterSuccess` or `clearAuthFormState` functions
- The `submitByKnob` or `logout` functions
- The return statement at the end of the store
  </action>
  <verify>
    <automated>cd frontend && grep -n "console.warn" src/stores/authStore.ts; test $? -eq 1 && echo "PASS: No console.warn found in authStore.ts" || echo "FAIL: console.warn lines remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "console.warn" frontend/src/stores/authStore.ts` returns no results (exit code 1)
    - The `initialize()` function still has try/catch structure with `authAdapter.initialize()` call
    - The `assignUser(currentUser)` call is preserved
    - The `authAdapter.onAuthStateChange()` callback is preserved with `assignUser(nextUser)` and `if (nextUser)` block
    - `npm run build` (or `npx vue-tsc --noEmit`) passes without TypeScript errors from authStore.ts
  </acceptance_criteria>
  <done>All 4 console.warn lines removed from authStore.ts initialize() function</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information Disclosure | authStore.ts console.warn | mitigate | The 4 console.warn lines logged JWT-authenticated user state to the browser console in production. Removing these lines eliminates the information disclosure. No replacement logging needed — these were debug-only trace statements. |
</threat_model>

<verification>
Verification:
1. Run `grep -n "console.warn" frontend/src/stores/authStore.ts` — should return exit code 1 (no matches)
2. Run `npx vue-tsc --noEmit` from frontend/ to verify no TypeScript errors
3. Manually verify the `initialize()` function still works by logging in through the app
</verification>

<success_criteria>
- `grep "console.warn" frontend/src/stores/authStore.ts` returns zero matches
- `initialize()` function logic is unchanged (try/catch, assignUser, onAuthStateChange all intact)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-non-tree-optimization/01-04-SUMMARY.md`
</output>
